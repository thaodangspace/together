{
    "name": "syncwatch",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
        "dev": "deno run --allow-net --allow-read --allow-write --allow-env astro dev",
        "start": "deno run --allow-net --allow-read --allow-write --allow-env astro start",
        "build": "deno run --allow-net --allow-read --allow-write --allow-env astro build",
        "preview": "deno run --allow-net --allow-read --allow-write --allow-env astro preview",
        "astro": "astro"
    },
    "dependencies": {
        "@astrojs/react": "^4.3.0",
        "@astrojs/tailwind": "^5.0.0",
        "astro": "^5.10.0",
        "axios": "^1.10.0",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "tailwindcss": "^3.3.0",
        "zustand": "^5.0.5"
    },
    "devDependencies": {
        "@types/react": "^18.2.0",
        "@types/react-dom": "^18.2.0"
    }
}